/**
 * @deepseek-ai/dsh-engine-session-demo — the experimental whole-session
 * engine runner. The package's substance is `src/bin.ts`, which boots the
 * `examples/engine-session` leaf and drives one durable harness session
 * entirely through a delegation backend; this module carries no runtime API.
 * @module @deepseek-ai/dsh-engine-session-demo
 */

export {}
